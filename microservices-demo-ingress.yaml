apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-external
spec:
  rules:
  - http:
      paths:
        - path: /
          backend:
            serviceName: frontend-external
            servicePort: 80